Title, abstract, keywords: regression AND (learning OR clustering OR logistic OR `support vector machine' OR `Neural Network' OR `bayes' OR `natural language processing' OR `K nearest neighbors') Title: `test' AND (`selection' OR `prioritization' OR `rank')