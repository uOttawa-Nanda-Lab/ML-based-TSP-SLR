TI (`test' AND (`selection'  OR  `prioritization' OR `rank')) AND TX (`regression'  AND (`learning' OR `clustering' OR `logistic' OR  `support vector machine' OR `Neural Network' OR  `bayes' OR `natural language processing' OR `K nearest neighbors' ))